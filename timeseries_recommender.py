""" Takes all available values in the time series and finds statistical 
parameters over a range of time such as gradient, range, etc and uses a 
lookup table which triggers a recommendation if any of these parameters are 
beyond a set limit."""

def recommend(inputs):
    return None